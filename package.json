{
  "name": "tapsdk-pc-js-monorepo",
  "private": true,
  "packageManager": "pnpm@10.28.2",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "typecheck": "pnpm -r run typecheck",
    "docs:dev": "pnpm --filter tapsdk-pc-docs dev",
    "docs:build": "pnpm --filter tapsdk-pc-docs build"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.0"
  }
}
